1. create a pipelie job
2. configure 3 build stages: checkout , build , end
3. in checkout:
	checkout geek-kb/scripts repository
   in build:
	count the number of files (recursivly)
   in end:
	display the pitput of: ls -lR 
